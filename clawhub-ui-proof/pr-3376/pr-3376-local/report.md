# ClawHub UI Proof

Status: pass

- Real ClawHub preview: `http://127.0.0.1:3014/`
- Public read backend, canonical Trending feed
- The `skills.sh` badge renders before the `19.6k` count.
- Hovering the badge renders the shared styled tooltip with `Synced from skills.sh`.
- The badge has no browser-native `title` attribute.
- Mobile verification at `390x844` showed no horizontal overflow.
