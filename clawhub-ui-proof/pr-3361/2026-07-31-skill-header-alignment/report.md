# ClawHub UI Proof

Status: pass

At 1440x900, the deployed baseline places `Skill` after the icon spacer and leaves `24h downloads` in the vacated category column. The candidate uses a two-column Trending header: `Skill` begins 4px inside the list edge and `24h downloads` ends 4px inside the opposite edge.

The candidate was also measured at 768x1024 and 390x844. Both labels remain visible at opposite edges with no horizontal document overflow.

